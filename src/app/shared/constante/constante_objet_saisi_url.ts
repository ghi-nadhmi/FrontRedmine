import { environment } from '@environments/environment';

const url = environment.apiUrl;

export const CONSTANTE_OBJET_SAISI_URL = {

    URL_OBJET_SAISI : `${url}/app/objetSaisi/`,
    URL_OBJET_SAISI_DATA : `${url}/app/objetSaisi/data`,

}